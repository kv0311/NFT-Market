{
  "scripts": {
    "build": "tsc",
    "test": "jest --setupFiles dotenv/config",
    "start": "rm -rf ./src/build && tsc && node src/build/dist",
    "encode": "rm -rf ./src/build && tsc && node src/build/dist/logic/script/encode.js",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "@nomiclabs/hardhat-etherscan": "^3.0.0",
    "@openzeppelin/contracts": "^4.4.2",
    "base64-sol": "^1.1.0",
    "dotenv": "^12.0.4",
    "hardhat": "^2.8.2",
    "typescript": "^4.5.5"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.4",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.5.3",
    "node-libpng": "^0.2.18"
  },
  "engines": {
    "node": ">=15.0.0"
  }
}
