pragma solidity ^0.8.6;
import './AllIn.sol';
library NFTUtils {
    function generateSeed(uint256 nounId, uint256 backgroundCount, uint256 bodyCount, uint256 accessoryCount, uint256 headCount, uint256 glassesCount) external view returns (AllIn.Seed memory) {
        uint256 pseudorandomness = uint256(
            keccak256(abi.encodePacked(blockhash(block.number - 1), nounId))
        );

        return AllIn.Seed({
            background: uint48(
                uint48(pseudorandomness) % backgroundCount
            ),
            body: uint48(
                uint48(pseudorandomness >> 48) % bodyCount
            ),
            accessory: uint48(
                uint48(pseudorandomness >> 96) % accessoryCount
            ),
            head: uint48(
                uint48(pseudorandomness >> 144) % headCount
            ),
            glasses: uint48(
                uint48(pseudorandomness >> 192) % glassesCount
            )
        });
    }
}